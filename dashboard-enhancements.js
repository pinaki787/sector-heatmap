(() => {
  const sectorRefreshMs = 30 * 60 * 1000;
  const accountRefreshMs = 10 * 1000;
  const money = value => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 2 }).format(Number(value || 0));
  const pct = value => `${Number(value || 0) >= 0 ? '+' : ''}${Number(value || 0).toFixed(2)}%`;
  const signed = value => Number(value || 0) >= 0 ? 'up' : 'down';

  document.body.innerHTML = `<style>
    :root{color-scheme:dark}body{margin:0;background:#09111f;color:#edf4ff;font:14px Inter,system-ui,sans-serif}.desk{min-height:100vh;display:grid;grid-template-columns:250px minmax(0,1fr);background:#09111f}.side{padding:24px 16px;background:#0d192a;border-right:1px solid #243a57;display:flex;flex-direction:column;gap:24px}.brand{padding:4px 10px}.brand b{font-size:18px}.brand small,.label,.muted{display:block;color:#8fa5c2;font-size:11px}.nav{display:grid;gap:7px}.nav button{border:0;border-radius:9px;padding:12px;text-align:left;background:transparent;color:#b3c4da;font-weight:700;cursor:pointer}.nav button.active,.nav button:hover{background:#18324e;color:white}.broker-card{margin-top:auto;padding:15px;border:1px solid #294766;border-radius:12px;background:#102138}.broker-card b{display:block;margin:8px 0}.dot{display:inline-block;width:8px;height:8px;border-radius:50%;background:#f2b74b;margin-right:7px}.dot.ok{background:#55d5a5;box-shadow:0 0 10px #55d5a5}.button{border:0;border-radius:8px;background:#2678be;color:white;padding:10px 12px;font-weight:800;cursor:pointer}.work{max-width:1500px;width:100%;margin:auto;padding:30px}.view{display:none}.view.active{display:block}.head{display:flex;justify-content:space-between;align-items:center;margin-bottom:20px}.head h1,.head h2{margin:0}.status{padding:9px 12px;border:1px solid #294766;border-radius:999px;background:#102138;font-size:12px;font-weight:700}.grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.metric,.panel{background:#101d31;border:1px solid #233c59;border-radius:14px}.metric{padding:16px}.metric strong{display:block;font-size:22px;margin-top:7px}.up{color:#60dca9}.down{color:#ff92a1}.sector-panel{margin-top:15px;padding:18px}.sector-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.sector{padding:14px;border:1px solid #ffffff1a;border-radius:10px;background:#173c36}.sector.down-sector{background:#482330}.sector b,.sector span{display:block}.sector span{font-size:24px;font-weight:800;margin-top:20px}.split{display:grid;grid-template-columns:1fr 1fr;gap:15px}.panel{padding:18px}.panel h2{font-size:17px}.row{display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr;gap:12px;padding:11px 0;border-top:1px solid #263c57;font-size:12px}.row.header{border-top:0;color:#91a7c4;font-size:10px;font-weight:800;text-transform:uppercase}.tabs{display:flex;gap:8px;margin:14px 0}.tabs button{border:1px solid #2b4a6a;border-radius:999px;background:transparent;color:#bfd0e4;padding:7px 10px;font-size:11px;cursor:pointer}.tabs button.active{background:#1d4c71;color:white}.empty{padding:22px 0;color:#90a4bd}.last{color:#8fa5c2;font-size:11px}@media(max-width:950px){.desk{grid-template-columns:1fr}.side{display:none}.grid,.sector-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.split{grid-template-columns:1fr}.work{padding:18px}}@media(max-width:560px){.grid,.sector-grid{grid-template-columns:1fr}.row{grid-template-columns:1.2fr .8fr .8fr}.row span:nth-child(3){display:none}}</style>
    <main class="desk"><aside class="side"><div class="brand"><b>◈ Sector Pulse</b><small>Trading workspace</small></div><nav class="nav"><button class="active" data-view="sectors">Sector-led analysis</button><button data-view="broker">Live feed & positions</button></nav><div class="broker-card"><span class="label">BROKER CONNECTION</span><b><i id="broker-dot" class="dot"></i><span id="broker-state">Checking Fyers</span></b><button class="button" id="reauth">Refresh authentication</button></div></aside><section class="work"><section id="sectors" class="view active"><header class="head"><div><span class="label">SECTOR-LED ANALYSIS</span><h1>Leadership map</h1></div><div class="status"><i id="feed-dot" class="dot"></i><span id="feed-state">Loading market feed</span></div></header><div class="grid"><article class="metric"><span class="label">Market breadth</span><strong id="breadth">—</strong></article><article class="metric"><span class="label">Leading sector</span><strong id="leader">—</strong></article><article class="metric"><span class="label">Sectors tracked</span><strong id="sector-count">—</strong></article><article class="metric"><span class="label">Last sector refresh</span><strong id="sector-time">—</strong></article></div><section class="panel sector-panel"><span class="label">SECTOR STRENGTH</span><h2>Ranked by intraday move</h2><div id="sector-grid" class="sector-grid"></div></section></section><section id="broker" class="view"><header class="head"><div><span class="label">LIVE FEED & POSITIONS</span><h1>Broker dashboard</h1></div><div class="status"><i id="account-dot" class="dot"></i><span id="account-state">Loading account</span></div></header><div class="grid"><article class="metric"><span class="label">Live P&L</span><strong id="live-pnl">—</strong></article><article class="metric"><span class="label">Open positions</span><strong id="open-count">—</strong></article><article class="metric"><span class="label">Available funds</span><strong id="funds">—</strong></article><article class="metric"><span class="label">Closed P&L</span><strong id="closed-pnl">—</strong></article></div><div class="split" style="margin-top:15px"><section class="panel"><span class="label">OPEN POSITIONS</span><h2>Current broker positions</h2><div class="row header"><span>Instrument</span><span>Quantity</span><span>Average</span><span>Live P&L</span></div><div id="open-positions"></div></section><section class="panel"><span class="label">CLOSED POSITIONS</span><h2>Realised P&L</h2><div class="tabs"><button class="active" data-period="daily">Today</button><button data-period="weekly">Week</button><button data-period="monthly">Month</button><button data-period="annual">Year</button></div><div class="row header"><span>Instrument</span><span>Buy / sell</span><span>Quantity</span><span>Realised P&L</span></div><div id="closed-positions"></div></section></div></section></section></main>`;

  const $ = id => document.getElementById(id);
  let period = 'daily';
  const setText = (id, text, cls) => { const node = $(id); node.textContent = text; node.className = cls || ''; };
  const renderSectors = data => {
    const sectors = (data.sectors || []).filter(item => item.change != null).sort((a, b) => b.change - a.change);
    const positives = sectors.filter(item => item.change > 0).length;
    setText('breadth', sectors.length ? `${positives} of ${sectors.length} positive` : 'Waiting');
    setText('leader', sectors[0] ? `${sectors[0].name} ${pct(sectors[0].change)}` : '—', sectors[0] ? signed(sectors[0].change) : '');
    setText('sector-count', String(sectors.length));
    setText('sector-time', data.updated_at ? new Date(data.updated_at).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) : '—');
    $('sector-grid').innerHTML = sectors.map(item => `<article class="sector ${item.change < 0 ? 'down-sector' : ''}"><b>${item.name}</b><small>${item.index}</small><span class="${signed(item.change)}">${pct(item.change)}</span><small>${item.weight}% index weight</small></article>`).join('') || '<p class="empty">Waiting for market data…</p>';
    $('feed-dot').classList.toggle('ok', !!data.connected); $('feed-state').textContent = data.connected ? 'Fyers feed connected' : 'Feed unavailable';
  };
  const refreshSectors = () => fetch('/api/heatmap', { cache: 'no-store' }).then(response => response.json()).then(renderSectors).catch(() => { $('feed-state').textContent = 'Feed unavailable'; });
  const refreshAccount = () => fetch('/api/account', { cache: 'no-store' }).then(response => response.json()).then(data => {
    const positions = (data.positions || []).filter(item => Number(item.netQty || item.net_qty || 0) !== 0);
    setText('live-pnl', money(data.pnl), signed(data.pnl)); setText('open-count', String(positions.length)); setText('funds', data.available_funds == null ? '—' : money(data.available_funds));
    $('open-positions').innerHTML = positions.length ? positions.map(item => { const qty = item.netQty ?? item.net_qty ?? 0, pnl = item.pl ?? item.pnl ?? 0; return `<div class="row"><b>${item.symbol || item.symbol_name || '—'}</b><span>${qty}</span><span>${money(item.netAvg ?? item.net_avg ?? 0)}</span><strong class="${signed(pnl)}">${money(pnl)}</strong></div>`; }).join('') : '<p class="empty">No open broker positions.</p>';
    ['broker-dot', 'account-dot'].forEach(id => $(id).classList.toggle('ok', !!data.connected)); $('broker-state').textContent = data.connected ? 'Fyers connected' : 'Fyers disconnected'; $('account-state').textContent = data.connected ? 'Broker feed connected' : 'Broker feed unavailable';
  }).catch(() => { $('account-state').textContent = 'Broker feed unavailable'; });
  const refreshClosed = () => fetch(`/api/realized-pnl?period=${period}`, { cache: 'no-store' }).then(response => response.json()).then(data => {
    const summary = data.summary || {}; setText('closed-pnl', money(summary.net_pnl), signed(summary.net_pnl));
    $('closed-positions').innerHTML = (data.records || []).length ? data.records.map(item => `<div class="row"><b>${item.symbol}</b><span>${item.buy_rate} / ${item.sell_rate}</span><span>${item.buy_qty} / ${item.sell_qty}</span><strong class="${signed(item.pnl)}">${money(item.pnl)}</strong></div>`).join('') : '<p class="empty">No closed positions in this period.</p>';
  }).catch(() => { $('closed-positions').innerHTML = '<p class="empty">Closed-position data is unavailable.</p>'; });
  document.querySelectorAll('[data-view]').forEach(button => button.addEventListener('click', () => { document.querySelectorAll('[data-view]').forEach(item => item.classList.toggle('active', item === button)); document.querySelectorAll('.view').forEach(view => view.classList.toggle('active', view.id === button.dataset.view)); }));
  document.querySelectorAll('[data-period]').forEach(button => button.addEventListener('click', () => { period = button.dataset.period; document.querySelectorAll('[data-period]').forEach(item => item.classList.toggle('active', item === button)); refreshClosed(); }));
  $('reauth').addEventListener('click', () => fetch('/api/auth/start').then(response => response.json()).then(data => window.open(data.url, '_blank', 'noopener')));
  refreshSectors(); refreshAccount(); refreshClosed(); setInterval(refreshSectors, sectorRefreshMs); setInterval(refreshAccount, accountRefreshMs);
})();
