(() => {
  const sectorRefreshMs = 30 * 60 * 1000;
  const accountRefreshMs = 10 * 1000;
  const money = value => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 2 }).format(Number(value || 0));
  const pct = value => `${Number(value || 0) >= 0 ? '+' : ''}${Number(value || 0).toFixed(2)}%`;
  const signed = value => Number(value || 0) >= 0 ? 'up' : 'down';

  document.body.innerHTML = `<style>
    :root{color-scheme:dark}body{margin:0;background:#09111f;color:#edf4ff;font:14px Inter,system-ui,sans-serif}.desk{min-height:100vh;display:grid;grid-template-columns:250px minmax(0,1fr);background:#09111f}.side{padding:24px 16px;background:#0d192a;border-right:1px solid #243a57;display:flex;flex-direction:column;gap:24px}.brand{padding:4px 10px}.brand b{font-size:18px}.brand small,.label,.muted{display:block;color:#8fa5c2;font-size:11px}.nav{display:grid;gap:7px}.nav button{border:0;border-radius:9px;padding:12px;text-align:left;background:transparent;color:#b3c4da;font-weight:700;cursor:pointer}.nav button.active,.nav button:hover{background:#18324e;color:white}.nav button:focus-visible,.button:focus-visible,.tabs button:focus-visible{outline:3px solid #a9dcff;outline-offset:2px}.broker-card{margin-top:auto;padding:15px;border:1px solid #294766;border-radius:12px;background:#102138}.broker-card b{display:block;margin:8px 0}.dot{display:inline-block;width:8px;height:8px;border-radius:50%;background:#f2b74b;margin-right:7px}.dot.ok{background:#55d5a5;box-shadow:0 0 10px #55d5a5}.button{border:0;border-radius:8px;background:#2678be;color:white;padding:10px 12px;font-weight:800;cursor:pointer}.work{max-width:1500px;width:100%;margin:auto;padding:30px}.view{display:none}.view.active{display:block}.head{display:flex;justify-content:space-between;align-items:center;margin-bottom:20px}.head h1,.head h2{margin:0}.status{padding:9px 12px;border:1px solid #294766;border-radius:999px;background:#102138;font-size:12px;font-weight:700}.grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.metric,.panel{background:#101d31;border:1px solid #233c59;border-radius:14px}.metric{padding:16px}.metric strong{display:block;font-size:22px;margin-top:7px}.up{color:#60dca9}.down{color:#ff92a1}.sector-panel{margin-top:15px;padding:18px}.sector-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.sector{font:inherit;color:#edf4ff;text-align:left;cursor:pointer;padding:14px;border:1px solid #ffffff1a;border-radius:10px;background:#173c36;transition:border-color .16s ease,transform .16s ease,filter .16s ease}.sector:hover{filter:brightness(1.1);transform:translateY(-2px)}.sector:focus-visible{outline:3px solid #a9dcff;outline-offset:2px}.sector.selected{border-color:#9dd9ff;box-shadow:inset 0 0 0 1px #9dd9ff}.sector.down-sector{background:#482330}.sector.unavailable{background:#17263a}.sector b,.sector span{display:block}.sector span{font-size:24px;font-weight:800;margin-top:20px}.sector-detail{margin-top:15px;padding:18px}.detail-head{display:flex;align-items:start;justify-content:space-between;gap:16px;margin-bottom:10px}.detail-head h2{margin:3px 0 0}.state{padding:22px 0;color:#90a4bd}.state.error{color:#ffb1bb}.split{display:grid;grid-template-columns:1fr 1fr;gap:15px}.panel{padding:18px}.panel h2{font-size:17px}.row{display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr;gap:12px;padding:11px 0;border-top:1px solid #263c57;font-size:12px}.row.header{border-top:0;color:#91a7c4;font-size:10px;font-weight:800;text-transform:uppercase}.tabs{display:flex;gap:8px;margin:14px 0;flex-wrap:wrap}.tabs button{border:1px solid #2b4a6a;border-radius:999px;background:transparent;color:#bfd0e4;padding:7px 10px;font-size:11px;cursor:pointer}.tabs button.active{background:#1d4c71;color:white}.empty{padding:22px 0;color:#90a4bd}.last{color:#8fa5c2;font-size:11px}@media(max-width:950px){.desk{grid-template-columns:1fr}.side{border-right:0;border-bottom:1px solid #243a57;padding:14px 18px;gap:12px}.brand{padding:0}.nav{grid-template-columns:repeat(2,minmax(0,1fr))}.nav button{text-align:center}.broker-card{margin-top:0}.grid,.sector-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.split{grid-template-columns:1fr}.work{padding:18px}}@media(max-width:560px){.grid,.sector-grid{grid-template-columns:1fr}.row{grid-template-columns:1.2fr .8fr .8fr}.row span:nth-child(3){display:none}.detail-head{display:block}.detail-head .status{display:inline-block;margin-top:10px}}</style>
    <main class="desk"><aside class="side"><div class="brand"><b>◈ Sector Pulse</b><small>Trading workspace</small></div><nav class="nav" aria-label="Dashboard views"><button type="button" class="active" data-view="sectors" aria-pressed="true">Sector-led analysis</button><button type="button" data-view="broker" aria-pressed="false">Live feed & positions</button></nav><div class="broker-card"><span class="label">BROKER CONNECTION</span><b role="status" aria-live="polite"><i id="broker-dot" class="dot" aria-hidden="true"></i><span id="broker-state">Checking Fyers</span></b><button type="button" class="button" id="reauth">Refresh authentication</button></div></aside><section class="work"><section id="sectors" class="view active"><header class="head"><div><span class="label">SECTOR-LED ANALYSIS</span><h1>Leadership map</h1></div><div class="status" role="status"><i id="feed-dot" class="dot" aria-hidden="true"></i><span id="feed-state">Loading market feed</span></div></header><div class="grid"><article class="metric"><span class="label">Market breadth</span><strong id="breadth">—</strong></article><article class="metric"><span class="label">Leading sector</span><strong id="leader">—</strong></article><article class="metric"><span class="label">Sectors tracked</span><strong id="sector-count">—</strong></article><article class="metric"><span class="label">Last sector refresh</span><strong id="sector-time">—</strong></article></div><section class="panel sector-panel"><span class="label">SECTOR STRENGTH</span><h2>Ranked by intraday move</h2><div id="sector-grid" class="sector-grid" aria-label="Sector heat map"><p class="state" role="status">Loading sectors…</p></div></section><section class="panel sector-detail" aria-live="polite"><div class="detail-head"><div><span class="label">SELECTED SECTOR</span><h2 id="driver-title">Choose a sector</h2></div><span id="driver-move" class="status">Select a sector to inspect stocks</span></div><div id="driver-content"><p class="state">Constituent data will appear here.</p></div></section></section><section id="broker" class="view"><header class="head"><div><span class="label">LIVE FEED & POSITIONS</span><h1>Broker dashboard</h1></div><div class="status" role="status"><i id="account-dot" class="dot" aria-hidden="true"></i><span id="account-state">Loading account</span></div></header><div class="grid"><article class="metric"><span class="label">Live P&L</span><strong id="live-pnl">—</strong></article><article class="metric"><span class="label">Open positions</span><strong id="open-count">—</strong></article><article class="metric"><span class="label">Available funds</span><strong id="funds">—</strong></article><article class="metric"><span class="label">Closed P&L</span><strong id="closed-pnl">—</strong></article></div><div class="split" style="margin-top:15px"><section class="panel"><span class="label">OPEN POSITIONS</span><h2>Current broker positions</h2><div class="row header"><span>Instrument</span><span>Quantity</span><span>Average</span><span>Live P&L</span></div><div id="open-positions"></div></section><section class="panel"><span class="label">CLOSED POSITIONS</span><h2>Realised P&L</h2><div class="tabs" aria-label="Realised P and L period"><button type="button" class="active" data-period="daily" aria-pressed="true">Today</button><button type="button" data-period="weekly" aria-pressed="false">Week</button><button type="button" data-period="monthly" aria-pressed="false">Month</button><button type="button" data-period="annual" aria-pressed="false">Year</button></div><div class="row header"><span>Instrument</span><span>Buy / sell</span><span>Quantity</span><span>Realised P&L</span></div><div id="closed-positions"></div></section></div></section></section></main>`;

  const $ = id => document.getElementById(id);
  let period = 'daily';
  let latestSectors = [];
  let selectedSectorName = '';
  const setText = (id, text, cls) => { const node = $(id); node.textContent = text; node.className = cls || ''; };
  const renderDrivers = () => {
    const sector = latestSectors.find(item => item.name === selectedSectorName);
    if (!sector) return;
    $('driver-title').textContent = `${sector.name} constituents`;
    $('driver-move').textContent = sector.change == null ? 'Sector move unavailable' : `Sector ${pct(sector.change)}`;
    $('driver-move').className = `status ${sector.change == null ? '' : signed(sector.change)}`;
    const drivers = sector.drivers || [];
    $('driver-content').innerHTML = drivers.length ? `<div class="row header"><span>Stock</span><span>Contribution</span><span>Move</span><span>Price</span></div>${drivers.map(item => `<div class="row"><b>${item.ticker}</b><span class="${item.contribution == null ? '' : signed(item.contribution)}">${item.contribution == null ? '—' : `${item.contribution >= 0 ? '+' : ''}${Number(item.contribution).toFixed(3)}`}</span><span class="${item.change == null ? '' : signed(item.change)}">${item.change == null ? '—' : pct(item.change)}</span><span>${item.price == null ? '—' : money(item.price)}</span></div>`).join('')}` : '<p class="state">No constituents are configured for this sector.</p>';
  };
  const renderSectors = data => {
    const sectors = [...(data.sectors || [])].sort((a, b) => (b.change ?? -Infinity) - (a.change ?? -Infinity));
    latestSectors = sectors;
    const positives = sectors.filter(item => item.change > 0).length;
    setText('breadth', sectors.length ? `${positives} of ${sectors.length} positive` : 'Waiting');
    setText('leader', sectors[0] ? `${sectors[0].name} ${pct(sectors[0].change)}` : '—', sectors[0] ? signed(sectors[0].change) : '');
    setText('sector-count', String(sectors.length));
    setText('sector-time', data.updated_at ? new Date(data.updated_at).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) : '—');
    $('sector-grid').innerHTML = sectors.map(item => `<button type="button" class="sector ${item.change == null ? 'unavailable' : item.change < 0 ? 'down-sector' : ''} ${selectedSectorName === item.name ? 'selected' : ''}" data-sector="${item.name}" aria-pressed="${selectedSectorName === item.name}"><b>${item.name}</b><small>${item.index}</small><span class="${item.change == null ? '' : signed(item.change)}">${item.change == null ? '—' : pct(item.change)}</span><small>${item.weight}% index weight · View stocks</small></button>`).join('') || `<p class="state ${data.error ? 'error' : ''}" role="status">${data.error || 'No sectors are available yet.'}</p>`;
    $('sector-grid').querySelectorAll('[data-sector]').forEach(button => button.addEventListener('click', () => { selectedSectorName = button.dataset.sector; renderSectors(data); renderDrivers(); }));
    if (selectedSectorName) renderDrivers();
    $('feed-dot').classList.toggle('ok', !!data.connected); $('feed-state').textContent = data.connected ? 'Fyers feed connected' : (data.error || 'Feed unavailable');
  };
  const refreshSectors = () => fetch('/api/heatmap', { cache: 'no-store' }).then(response => { if (!response.ok) throw new Error(`Market data request failed (${response.status})`); return response.json(); }).then(renderSectors).catch(error => { $('feed-state').textContent = 'Feed unavailable'; $('sector-grid').innerHTML = `<p class="state error" role="alert">${error.message || 'Sector data is unavailable.'}</p>`; });
  const refreshAccount = () => fetch('/api/account', { cache: 'no-store' }).then(response => response.json()).then(data => {
    const positions = (data.positions || []).filter(item => Number(item.netQty || item.net_qty || 0) !== 0);
    setText('live-pnl', money(data.pnl), signed(data.pnl)); setText('open-count', String(positions.length)); setText('funds', data.available_funds == null ? '—' : money(data.available_funds));
    $('open-positions').innerHTML = positions.length ? positions.map(item => { const qty = item.netQty ?? item.net_qty ?? 0, pnl = item.pl ?? item.pnl ?? 0; return `<div class="row"><b>${item.symbol || item.symbol_name || '—'}</b><span>${qty}</span><span>${money(item.netAvg ?? item.net_avg ?? 0)}</span><strong class="${signed(pnl)}">${money(pnl)}</strong></div>`; }).join('') : '<p class="empty">No open broker positions.</p>';
    ['broker-dot', 'account-dot'].forEach(id => $(id).classList.toggle('ok', !!data.connected)); $('broker-state').textContent = data.connected ? 'Fyers connected' : (data.error || 'Fyers disconnected'); $('account-state').textContent = data.connected ? 'Broker feed connected' : (data.error || 'Broker feed unavailable');
  }).catch(() => { $('account-state').textContent = 'Broker feed unavailable'; });
  const refreshClosed = () => fetch(`/api/realized-pnl?period=${period}`, { cache: 'no-store' }).then(response => response.json()).then(data => {
    const summary = data.summary || {}; setText('closed-pnl', money(summary.net_pnl), signed(summary.net_pnl));
    $('closed-positions').innerHTML = (data.records || []).length ? data.records.map(item => `<div class="row"><b>${item.symbol}</b><span>${item.buy_rate} / ${item.sell_rate}</span><span>${item.buy_qty} / ${item.sell_qty}</span><strong class="${signed(item.pnl)}">${money(item.pnl)}</strong></div>`).join('') : '<p class="empty">No closed positions in this period.</p>';
  }).catch(() => { $('closed-positions').innerHTML = '<p class="empty">Closed-position data is unavailable.</p>'; });
  document.querySelectorAll('[data-view]').forEach(button => button.addEventListener('click', () => { document.querySelectorAll('[data-view]').forEach(item => { const active = item === button; item.classList.toggle('active', active); item.setAttribute('aria-pressed', String(active)); }); document.querySelectorAll('.view').forEach(view => view.classList.toggle('active', view.id === button.dataset.view)); }));
  document.querySelectorAll('[data-period]').forEach(button => button.addEventListener('click', () => { period = button.dataset.period; document.querySelectorAll('[data-period]').forEach(item => { const active = item === button; item.classList.toggle('active', active); item.setAttribute('aria-pressed', String(active)); }); refreshClosed(); }));
  $('reauth').addEventListener('click', async () => {
    const button = $('reauth');
    const loginWindow = window.open('about:blank', 'fyers-oauth');
    button.disabled = true;
    button.textContent = 'Opening Fyers login…';
    try {
      const response = await fetch('/api/auth/start', { cache: 'no-store' });
      const data = await response.json();
      if (!response.ok || !data.url) throw new Error(data.error || 'Could not start Fyers authentication');
      if (loginWindow) {
        loginWindow.opener = null;
        loginWindow.location.replace(data.url);
      } else {
        window.location.assign(data.url);
      }
      $('broker-state').textContent = 'Complete login and 2FA in the browser';
    } catch (error) {
      if (loginWindow) loginWindow.close();
      $('broker-state').textContent = error.message;
    } finally {
      button.disabled = false;
      button.textContent = 'Refresh authentication';
    }
  });
  refreshSectors(); refreshAccount(); refreshClosed(); setInterval(refreshSectors, sectorRefreshMs); setInterval(refreshAccount, accountRefreshMs);
})();
